language: python
python:
  - '2.7'
  - pypy
install:
  - pip install --upgrade pip
  - pip install -r requirements-dev.txt
  - pip install -e .
script:
  - py.test unicore
after_success:
  - coveralls
